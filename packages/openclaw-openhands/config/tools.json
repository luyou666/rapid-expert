{
  "license": "MIT",
  "disclaimer": "For learning, research, source organization, and workflow assistance only. Not professional advice.",
  "tools": [
    {
      "name": "rapid_expert_harness_run",
      "argv": ["python", "scripts/harness.py", "run", "--task", "{task_json}", "--session-id", "{session_id}"],
      "description": "Run the full Rapid Expert Harness loop for a task JSON."
    },
    {
      "name": "rapid_expert_harness_step",
      "argv": ["python", "scripts/harness.py", "step", "--task", "{task_json}", "--session-id", "{session_id}", "--name", "{tool_name}"],
      "description": "Run one harness step for resumable agent orchestration."
    },
    {
      "name": "rapid_expert_harness_status",
      "argv": ["python", "scripts/harness.py", "status", "--session-id", "{session_id}"],
      "description": "Read harness session status and output list."
    },
    {
      "name": "rapid_expert_mcp_stdio",
      "argv": ["python", "scripts/harness_mcp.py"],
      "description": "Start the dependency-free JSON-RPC/MCP-style stdio wrapper."
    },
    {
      "name": "rapid_expert_queue_submit",
      "argv": ["python", "scripts/harness_queue.py", "submit", "--task", "{task_json}"],
      "description": "Submit a task to the file-backed harness queue."
    },
    {
      "name": "rapid_expert_queue_run_next",
      "argv": ["python", "scripts/harness_queue.py", "run-next"],
      "description": "Run the next queued harness job."
    },
    {
      "name": "rapid_expert_queue_worker",
      "argv": ["python", "scripts/harness_queue.py", "worker", "--max-jobs", "{max_jobs}", "--stop-when-empty"],
      "description": "Run queued harness jobs through the file-backed worker."
    },
    {
      "name": "rapid_expert_queue_status",
      "argv": ["python", "scripts/harness_queue.py", "status", "--job-id", "{job_id}"],
      "description": "Read a queued harness job."
    },
    {
      "name": "rapid_expert_queue_approve",
      "argv": ["python", "scripts/harness_queue.py", "approve", "--job-id", "{job_id}", "--tool", "{tool_name}"],
      "description": "Approve a gated tool for a queued harness job."
    },
    {
      "name": "rapid_expert_queue_cancel",
      "argv": ["python", "scripts/harness_queue.py", "cancel", "--job-id", "{job_id}"],
      "description": "Cancel a non-running queued harness job."
    },
    {
      "name": "rapid_expert_queue_retry",
      "argv": ["python", "scripts/harness_queue.py", "retry", "--job-id", "{job_id}"],
      "description": "Requeue a failed or cancelled harness job if retry attempts remain."
    },
    {
      "name": "rapid_expert_health",
      "argv": ["python", "scripts/harness_diag.py", "health"],
      "description": "Run harness health diagnostics."
    },
    {
      "name": "rapid_expert_metrics",
      "argv": ["python", "scripts/harness_diag.py", "metrics"],
      "description": "Read queue and health metrics."
    },
    {
      "name": "rapid_expert_export_package",
      "argv": ["python", "scripts/harness_diag.py", "export", "--output", "{output_zip}"],
      "description": "Export a clean deployment zip without runtime outputs."
    },
    {
      "name": "rapid_expert_write_contracts",
      "argv": ["python", "scripts/harness_contract.py", "write"],
      "description": "Write OpenAPI and MCP tools contracts under deploy/."
    },
    {
      "name": "rapid_expert_rag_search",
      "argv": ["python", "scripts/rag_index.py", "search", "--index", "{rag_index_json}", "--query", "{query}", "--limit", "{limit}"],
      "description": "Search the lightweight RAG index generated by the harness."
    },
    {
      "name": "rapid_expert_risk",
      "argv": ["python", "scripts/rapid_expert.py", "risk", "--domain", "{domain}", "--question", "{question}"],
      "description": "Classify high-risk and unsafe requests before execution."
    },
    {
      "name": "rapid_expert_plan",
      "argv": ["python", "scripts/rapid_expert.py", "plan", "--domain", "{domain}", "--user-level", "{user_level}", "--daily-time", "{daily_time}", "--goal", "{goal}", "--output", "outputs/plan.json"],
      "description": "Choose a 5, 7, 9, or 12 day learning route."
    },
    {
      "name": "rapid_expert_scan",
      "argv": ["python", "scripts/rapid_expert.py", "scan", "--domain", "{domain}", "--question", "{question}", "--safe-mode", "--output", "outputs/sources_raw.json"],
      "description": "Collect current source candidates."
    },
    {
      "name": "rapid_expert_rank",
      "argv": ["python", "scripts/rapid_expert.py", "rank", "--input", "outputs/sources_raw.json", "--output", "outputs/sources_ranked.json"],
      "description": "Rank sources by heuristic credibility."
    },
    {
      "name": "rapid_expert_build",
      "argv": ["python", "scripts/rapid_expert.py", "build", "--domain", "{domain}", "--sources", "outputs/sources_ranked.json", "--duration", "{duration}", "--user-level", "{user_level}", "--output", "outputs/domain_kit_report.md"],
      "description": "Build a domain kit report skeleton."
    },
    {
      "name": "rapid_expert_evaluate",
      "argv": ["python", "scripts/rapid_expert.py", "evaluate", "--report", "outputs/domain_kit_report.md", "--sources", "outputs/sources_ranked.json", "--output", "outputs/evaluation.json"],
      "description": "Evaluate the generated deliverable."
    }
  ]
}
